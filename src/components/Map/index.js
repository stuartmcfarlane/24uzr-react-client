import React from 'react';

class Map extends React.Component {
  render() {
    return (
      <div className="Map" style={mapStyle}>
      </div>
    );
  }
}

const mapStyle = {};

export default Map;
